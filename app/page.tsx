/*
 * Home — the FutureAnalytics × Sycamore Square Group proposal. A co-branded
 * proposal "app" with a left section nav; the live OS mockup is embedded in
 * the "Live demo" section (iframe -> /app) with an "open full screen" link.
 */
import { ProposalShell } from "@/components/proposal/proposal-shell";

export default function Page() {
  return <ProposalShell />;
}
